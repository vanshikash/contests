#include <bits/stdc++.h>
using namespace std;

bool isBad(char arr[int n])
{
    int count = 0;
    for (int i = 0; i < n; i++)
    {
        if (arr[i] =='1'
    }
    {
        count++;
    }
    if (count % 2 != 0)
    {
        return true;
    }
    else
        return false;
}


int main()
{
    int t = 1;
    cin >> t;
    while (t--)
    {
        // int x;
        // char xd[1000];
        // cin >> x;
        // itoa(x, xd, 2);
        // cout << xd << endl;

        
        
    }
    return 0;
}