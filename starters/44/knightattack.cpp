#include <bits/stdc++.h>
using namespace std;

void FAST(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

int solve(int x1, int y1, int x2, int y2)
{
    
}

int main()
{
    FAST();
    OJ();
    int t = 1;
    cin >> t;
    while(t --)
    {
        int x1,y1,x2,y2;
        cin>>x1>>y1;
        cin>>x2>>y2;
        solve(x1,y1,x2,y2);
    }
    return 0;
}